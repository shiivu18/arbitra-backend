version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: arbitra-postgres
    environment:
      POSTGRES_DB: arbitra_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secretpassword
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - arbitra-net

  backend:
    build: .
    container_name: arbitra-backend
    ports:
      - "8084:8084"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/arbitra_db
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=secretpassword
      - SPRING_JPA_HIBERNATE_DDL_AUTO=validate
    depends_on:
      - postgres
    networks:
      - arbitra-net

volumes:
  postgres_data:

networks:
  arbitra-net:
    driver: bridge